# This file configures npm's network behavior, which is especially useful
# in CI/CD environments where network can sometimes be slow or unreliable.
# Set a longer timeout for network requests (in milliseconds).
# This was previously 'network-timeout' in older npm versions.
fetch-timeout=300000

# Configure retry behavior for failed network requests.
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-retries=5